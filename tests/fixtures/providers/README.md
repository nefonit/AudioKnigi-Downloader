# Provider fixtures

Small, deterministic HTML/PlayerJS snapshots used by parser regression tests.
They contain no copyrighted audiobook media and no live credentials. Add a new
fixture whenever a supported site's markup change causes a parser regression.
